class Solution {
    public boolean isPowerOfFour(int n) {
        if(n==1)return true;

        return check(n,1);

    }
    static boolean check(int n,int m){
        double p = Math.pow(4,m);
        if(p>n)return false;
        if(p==n)return true;
        return check(n,++m);
    }
}